# Calculator app

This was a practice application, the intent to make a working calculator using vanilla js, css and html.
The app is only for local use.

### To use:
- Clone repository
- Move in to root folder of repository
- Enter `open index.html` in your terminal
